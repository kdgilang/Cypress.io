version: 2.1

jobs:
  k6-build:
    docker:
      - pull loadimpact/k6:latest

    steps:
      - attach_workspace:
        at: ~/
      - run: npm run k6-performance-test

workflows:
  version: 2
  build-and-test:
    jobs:
      - k6-build